<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.VIBRATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Prim8mobile"
        tools:targetApi="31">

        <!-- DATABASE ACTIVITY -->
        <activity android:name=".MonkeyDBActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <provider android:name="MonkeyDBProvider"
            android:authorities="com.monkeydbs.MonkeyDBProvider"/>

        <!-- MAIN TAB ACTIVITY -->
        <activity android:name=".TabSwitcher" android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- TAB ACTIVITIES (4) -->
        <activity android:name=".FollowsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"
            android:configChanges="orientation|keyboardHidden"/>

        <activity android:name=".FollowsStartActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"
            android:configChanges="orientation|keyboardHidden"/>

        <activity android:name=".EthogramActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".IndividualsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ExportActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>


        <!-- SUB ACTIVITIES -->
        <activity android:name=".BehaviorsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviorsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ReplaceIndividualActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviorinstancesTextEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".IndividualsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".AboutActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".SitesEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ObserversEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".GroupsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BiologicalsamplesEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".CoordinatesystemEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".LocationsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".WorkcalendarsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviortypesEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".FocalfollowsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ScansEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".SpeciesEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviorinstancesEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".FocalbehaviorsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ScanbehaviorsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ModifiersEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".AdLibEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <!-- ACTIVITIES -->
        <activity android:name=".SitesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ObserversActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".GroupsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BiologicalsamplesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".CoordinatesystemActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".LocationsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".WorkcalendarsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviortypesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".FocalfollowsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ScansActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".SpeciesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".BehaviorinstancesActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".FocalbehaviorsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ScanbehaviorsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ModifiersActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".AdLibActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifierlistsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifiersActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifierlistbehaviorsActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifierlistsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifiersEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>

        <activity android:name=".ValidmodifierlistbehaviorsEditActivity"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar"/>
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:grantUriPermissions="true"
            android:exported="false">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/filepaths"/>
        </provider>
    </application>

</manifest>